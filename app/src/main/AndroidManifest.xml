<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.adbcontroller">

    <!-- Разрешения -->
    <uses-feature android:name="android.hardware.telephony"
        android:required="false" />
    <uses-permission android:name="android.permission.SEND_SMS" />
    <uses-permission android:name="android.permission.READ_SMS" />
    <uses-permission android:name="android.permission.RECEIVE_SMS" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- Нужны для GPS-спуфинга -->
    <uses-permission
        android:name="android.permission.ACCESS_MOCK_LOCATION"
        tools:ignore="MockLocation,ProtectedPermissions" />

    <uses-permission
        android:name="android.permission.WRITE_SECURE_SETTINGS"
        tools:ignore="ProtectedPermissions" />

    <!-- Foreground Service для Android 9+ -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ADBSystem">

        <!-- Активити можно оставить выключенной -->
        <activity
            android:name="com.adbcontroller.MainActivity"
            android:exported="true"
            android:enabled="false" />

        <!-- Ресивер для ADB команд -->
        <receiver
            android:name="com.adbcontroller.adb.AdbCommandReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="com.adbcontroller.ADB_COMMAND" />
            </intent-filter>
        </receiver>

        <!-- Автозапуск при перезагрузке -->
        <receiver
            android:name="com.adbcontroller.adb.BootReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!-- Новый ресивер после установки/обновления -->
        <receiver
            android:name="com.adbcontroller.adb.InstallReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
                <action android:name="android.intent.action.PACKAGE_REPLACED" />
                <data android:scheme="package"
                    android:path="com.adbcontroller" />
            </intent-filter>
        </receiver>

        <!-- ForegroundService ДОЛЖЕН БЫТЬ exported=true -->
        <service
            android:name="com.adbcontroller.adb.AdbForegroundService"
            android:enabled="true"
            android:exported="true"
            android:foregroundServiceType="dataSync|location|connectedDevice" />

    </application>

</manifest>